# 筛选条件配置
logic: all  # 匹配模式: all(所有条件) 或 any(任一条件)
limit: 100  # 结果数量限制 (0表示无限制)
output_fields:  # 输出字段 (可选)
  - name
  - uid
  - ccf_level
  - matched_records

conditions:
  - contest_type: "CSP入门"
    award: "一等奖"
    province: "上海"  # 省份限制 (可选)
    year_range: [2024, 2024]  # 年份范围 (可选)